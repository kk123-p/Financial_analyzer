# Frontend Archive

This directory contains the old SPA frontend (HTML/CSS/JS modules) that was replaced by server-rendered Jinja2 templates.

Archived on: 2026-05-28

Reason: Migrated from SPA + Plotly to server-rendered templates + ECharts for lighter footprint and simpler architecture.
