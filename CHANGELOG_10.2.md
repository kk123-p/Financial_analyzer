# 版本 10.2 改动说明

## 改动日期
2026-05-14 22:21

## 改动内容

### 1. AI体检报告/矛盾信号检测改为手动触发
- **问题**：点击侧边栏一键触发，用户无法控制
- **修改**：改为先进入AI投研标签页，显示数据预览，用户确认后再点击"开始分析"触发

### 2. AI智能分析输出美化
- **问题**：输出杂乱无条理
- **修改**：优化输出格式，添加分隔线、标签、结构化展示

### 3. 行业对比修复
- **问题**：`compare_with_peers()` 缺少 `peers_data` 参数
- **修改**：改为调用 `analyze()` 方法（内部自动处理行业对比）

### 4. 相对估值、股东回报、财报质量修复
- **问题**：方法调用方式不正确
- **修改**：统一使用 `analyze()` 入口方法，内部路由到各子分析

## 修改文件
- `financial_analyzer/ui/app.py`
- `financial_analyzer/ui/deepseek_dialog.py`
